# docker-compose 변수 기본값. 바꾸려면 이 파일을 .env 로 복사해 수정한다 (.env 는 커밋하지 않는다)
# 애플리케이션 환경변수(DATABASE_PASSWORD 등)는 local.env 에 있다

# Cassandra 컨테이너
CASSANDRA_IMAGE_TAG=5.0
CASSANDRA_PORT=9042
CASSANDRA_MAX_HEAP_SIZE=512M
CASSANDRA_HEAP_NEWSIZE=128M
